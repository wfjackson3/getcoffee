<%= twitter_bootstrap_form_for(@invite_status) do |f| %>
  <% if @invite_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invite_status.errors.count, "error") %> prohibited this invite_status from being saved:</h2>

      <ul>
      <% @invite_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
