<%= twitter_bootstrap_form_for(@event_tag) do |f| %>
  <% if @event_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_tag.errors.count, "error") %> prohibited this event_tag from being saved:</h2>

      <ul>
      <% @event_tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :tag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
