<h1>Listing event_tags</h1>

<table class="table">
  <tr>
    <th>Tag</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @event_tags.each do |event_tag| %>
  <tr>
    <td><%= event_tag.tag %></td>
    <td><%= link_to 'Show', event_tag %></td>
    <td><%= link_to 'Edit', edit_event_tag_path(event_tag) %></td>
    <td><%= link_to 'Destroy', event_tag, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Event tag', new_event_tag_path %>
